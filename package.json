{
  "name": "rolecall-foundry",
  "version": "0.1.0",
  "description": "Foundry VTT module that pulls RoleCall campaign scenes into the world as journal entries.",
  "homepage": "https://rolecall.games",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "lint": "tsc -noEmit -skipLibCheck"
  },
  "keywords": ["foundry-vtt", "rolecall", "ttrpg"],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.25.5",
    "typescript": "^5.8.3"
  }
}
